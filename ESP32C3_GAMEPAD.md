# ESP32-C3 ESP-NOW 手柄

## 烧录环境

LCDWIKI 主机使用：

```bash
pio run -e lcdwiki-es3c28p
```

ESP32-C3 手柄使用独立环境：

```bash
pio run -e esp32c3-gamepad
```

原始 ST7789 主机使用：

```bash
pio run -e esp32s3-n16r8
```

## 按键接线

当前手柄固件使用 8 个独立数字按键。所有按键均启用内部上拉，按键一端接
对应 GPIO，另一端接 GND；按下时为低电平。

## 蓝色 LED 状态

默认使用 GPIO8 的蓝色 LED：

| 状态 | LED |
| --- | --- |
| 未连接主机 | 慢闪 |
| 已连接主机且待机 | 很慢轻闪 |
| 摇杆或按键有输入 | 常亮 |
| 配对模式 | 快闪 |

如果 LED 亮灭反了，修改 `platformio.ini`：

```ini
-DDIJI_GAMEPAD_LED_ACTIVE_LEVEL=0
```

如果不使用 LED：

```ini
-DDIJI_GAMEPAD_LED_PIN=-1
```

## 默认按键引脚

默认按键使用 `INPUT_PULLUP`，按键另一端接 GND。

| 功能 | GPIO |
| --- | --- |
| A | 10 |
| B | 4 |
| SELECT | 5 |
| START | 6 |
| UP | 0 |
| DOWN | 1 |
| LEFT | 3 |
| RIGHT | 7 |
| 蓝色 LED | 8 |

如果实际接线不同，修改 `platformio.ini` 里 `[env:esp32c3-gamepad]` 的 `DIJI_GAMEPAD_*_PIN`。

## 配对流程

1. 主机开机后在主菜单底部点 `手柄`。
2. 选择 `配对 P1` 或 `配对 P2`。
3. 在 15 秒内同时长按 ESP32-C3 手柄的 `SELECT + START`，约 1.5 秒。
4. 主机显示 `P1 已配对` 或 `P2 已配对` 后，会保存手柄 MAC 地址。

P1 会和主机板载按键、电脑串口键盘输入合并；P2 会作为 NES 的 2 号手柄输入。

## 主机连接状态

主机配对成功后会发送 `PairAck`，之后每秒给已配对手柄发送一次 `HostHeartbeat`。

手柄 2.2 秒内收到主机包就认为已连接；超过这个时间没有收到，就回到未连接慢闪。

主机主菜单顶部会显示：

| 显示 | 含义 |
| --- | --- |
| `P1:ON 8ms` / `P2:ON 8ms` | 已配对且在线；后半部分是平滑后的单向通信延迟 |
| `P1:ON --ms` / `P2:ON --ms` | 已在线，正在等待首次时间同步 |
| `P1:--` / `P2:--` | 已配对但当前离线 |
| `P1:NO` / `P2:NO` | 未配对 |

协议 v4 会在状态包中携带递增序号和按键采样时间；主机会丢弃重复或倒序包。配对成功后，手柄也会把主机 MAC 写入 NVS，后续只接受该主机发送的心跳、时间同步和震动包。配对信息的持久化在主循环执行，不在 ESP-NOW 回调中执行。

## 通信延迟测试

进入主机“设置 -> 手柄”，选中 P1 或 P2 后：

- `A`：进入配对。
- `SELECT`：测试震动。
- `START`：开始通信测试。

通信测试连续发送 20 个 Ping，手柄立即返回 Pong。主机显示当前、最低、
平均和最高往返时间（RTT），以及未收到响应的样本数量。建议参考：

- 平均 RTT 不高于 30ms。
- 最高 RTT 不高于 80ms。
- 20 个样本最多丢失 1 个。

测试值是完整的主机到手柄再返回主机的往返时间，不是单向按键延迟。
如果数值随运行时间明显升高，重点检查同信道 Wi-Fi 流量、2.4GHz 干扰、
供电稳定性，以及手柄串口中的发送失败和发送回调耗时。

主界面顶部的 `P1:ON 8ms` 使用另一套持续测量机制：手柄连接后，主机立即
执行四时间戳同步，并约每 10 秒重新校准一次两块 MCU 的 `millis()` 偏移。
手柄每个状态包都携带本次实体按键采样时间，主机把它换算为自己的时间基准，
得到“手柄采样到主机收到”的单向延迟，并进行轻度平滑后显示。同步会优先采用
较低 RTT 的样本，以降低 Wi-Fi 瞬时排队对时钟偏移估算的影响。

顶部数值反映无线状态包到达主机的时间，不包含游戏下一帧读取输入、画面渲染
和屏幕响应时间；因此它用于判断通信链路，不等于从手指按下到屏幕变化的完整
端到端延迟。

## 手柄串口诊断

手柄串口速率为 `115200`。固件每两秒输出一次通信汇总，并在连接状态或
按键发生变化时额外输出一行。汇总字段包括：

- 当前连接状态、Wi-Fi 信道和距离最近主机包的时间。
- 已成功提交的状态包数量。
- 因上一包仍在发送而跳过的过期状态数量。
- ESP-NOW 立即入队错误数量。
- 无线发送成功/失败回调数量。
- 最长发送回调时间和最大的状态包发送间隔。
- 当前按键位图。

状态上报采用“单包在途”策略：上一包没有完成时不继续堆积旧状态，
下一次发送直接使用最新按键值。这可以避免无线环境变差后旧方向状态在
队列中排队，造成按键松开或转向延迟。

## Wi-Fi 与手柄同时使用

主机开机后会连接已保存的 2.4GHz Wi-Fi 并保持连接，ESP-NOW 直接复用路由器当前信道，不再为进入商店或云存档而反复关闭手柄。手柄丢失主机心跳后会自动扫频，优先检查 1、6、11，再检查其余 1～13 信道；发现主机后锁定并保存信道，无需重新配对或手工固定路由器信道。

已配对手柄的普通按键状态使用单播，广播只用于配对和信道发现。首次连接 Wi-Fi、路由器改变信道或主机漫游时，手柄可能短暂离线，随后应自动恢复。ROM 大文件下载和 OTA 会与手柄共享同一个 2.4GHz 射频，真机测试时仍需检查高流量场景的按键延迟与丢包。

协议 v4 改变了按键数据包结构，升级时必须同时刷新主机固件和 ESP32-C3 手柄固件；旧版按键协议与 v4 不会互相接受数据包。原有配对 MAC 会保留，但首次启动后若无法自动找回，可重新执行一次配对。

## 手柄自动 OTA

`esp32c3-gamepad` 使用双应用分区。首次需要通过 USB 烧录带 OTA
能力的手柄固件；以后手柄固件可以由主机统一检查并自动升级。

升级流程：

1. 手柄每次连接已配对主机后，通过独立的管理协议上报当前固件版本。
2. 用户在主机上执行“系统更新”时，主机把在线手柄版本与自己的版本放在
   同一个 OTA 检查请求中。
3. 服务端同时返回主机固件和 `esp32c3-gamepad` 固件信息。
4. 只有主机确认某个手柄存在更高版本后，才通过 ESP-NOW 单播发送升级
   申请、目标版本、HTTPS 下载地址、文件大小和 SHA-256。
5. 主机再发送当前家庭 2.4GHz Wi-Fi 的 SSID 和密码。手柄不会自行访问
   OTA 检查接口，只会下载主机指定的固件。
6. 手柄连接家庭 Wi-Fi，校验大小和 SHA-256，写入备用 OTA 分区并重启。
   下载、校验或写入失败时继续运行原固件。

主机与手柄之间的管理协议使用独立 magic/version，不跟随按键协议版本
变化，以避免主机升级按键协议后无法通知旧手柄 OTA。

当前方案按产品决策不启用 ESP-NOW 加密，Wi-Fi 密码只向已经配对的手柄
MAC 单播且不写入日志。附近具备抓包条件的设备仍可能监听到明文凭据。
固件传输使用 HTTPS，并同时执行 SHA-256 校验。

## 电量检测

默认 `DIJI_GAMEPAD_BATTERY_PIN=-1`，表示硬件未接入电量采样，主机显示在线状态但不显示百分比。若电池经 1:1 分压接到 ADC1 引脚，可在 `[env:esp32c3-gamepad]` 增加：

```ini
-DDIJI_GAMEPAD_BATTERY_PIN=2
```

固件按分压后的读数乘 2。为避免每个 60Hz 状态包都启动 ADC，电量改为每
10 秒采样一次，每次连续读取 8 个样本取平均，再按锂电池放电曲线分段换算
为 0～100%。如果使用不同分压比或电池化学体系，应同步调整
`updateBatterySample()`，不要把锂电池直接接到 GPIO。

1:1 的 100kΩ + 100kΩ 分压会持续消耗约 21μA。若产品需要较低待机功耗，
建议使用 220kΩ + 220kΩ 并在 ADC 输入端保留 0.1μF 电容，或者使用 MOSFET
仅在测量时接通分压回路。

## 功耗模式与自动休眠

手柄固件根据使用状态自动切换通信频率：

| 状态 | 行为 |
| --- | --- |
| 正在操作或操作后 10 秒内 | 约 60Hz 状态上报，保持最低按键延迟 |
| 已连接且空闲超过 10 秒 | 降到 10Hz；任何按键变化仍立即上报 |
| 主机离线 | 快速扫描一轮 1～13 信道，然后暂停 1.5 秒再扫描 |
| 无按键操作 5 分钟 | 进入 Deep-sleep |

Deep-sleep 会关闭无线连接，按下支持深睡唤醒的按键后重新启动，并从 NVS
恢复主机 MAC 和上次信道。ESP32-C3 当前可用于此功能的是 GPIO0～5，因此
按默认接线可唤醒手柄的按键为：

- `UP`（GPIO0）
- `DOWN`（GPIO1）
- `LEFT`（GPIO3）
- `B`（GPIO4）
- `SELECT`（GPIO5）

`A`（GPIO10）、`START`（GPIO6）和 `RIGHT`（GPIO7）可以正常游戏，但不能
从 Deep-sleep 唤醒。量产版如果要求任意键唤醒，需要重新分配按键引脚，或
增加独立电源键控制 ESP32-C3 的 `EN`/电源负载开关。

串口会输出当前功耗模式、状态上报统计、缓存电量，以及进入深睡和唤醒原因。
固件仍在游戏活动阶段保持 `WiFi.setSleep(false)`，不会为了省电牺牲正在游戏
时的 ESP-NOW 接收延迟。

## 震动马达

默认 `DIJI_GAMEPAD_RUMBLE_PIN=-1`，不会驱动马达。硬件完成 MOSFET/三极管、续流二极管和共地后，可配置一个支持 PWM 的 GPIO：

```ini
-DDIJI_GAMEPAD_RUMBLE_PIN=9
```

Rumble 包包含独立序号、0～255 强度和最长 5000ms 时长。手柄用非阻塞截止时间关闭 PWM，不会暂停 60Hz 按键上报。主机当前在配对测试、存档成功和低电量时提供反馈。

ESP-NOW 单播加密默认未启用：量产硬件需要安全注入共享 PMK/LMK 后再打开 peer encryption，密钥不得硬编码提交到仓库。当前 v4 已提供双向 MAC 身份绑定、动态信道发现、重复包过滤和时间同步，但它们不能替代加密。
