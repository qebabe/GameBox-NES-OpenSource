# GameBox LCDWIKI ES3C28P 用户使用教程

本文面向拿到成品或套件的普通用户，说明如何为 GameBox 主机首次烧录固件、准备 SD 卡、连接 Wi-Fi、配对无线手柄并开始游戏。

## 1. 适用设备与固件

本教程适用于：

- LCDWIKI ES3C28P / ES3N28P 开发板；
- ESP32-S3-N16R8，16 MB Flash、8 MB PSRAM；
- 320×240 ILI9341 屏幕；
- FT6336 电容触摸；
- 板载 ES8311 音频 Codec 和功放；
- 板载 SDIO MicroSD 卡槽。

主机首次烧录文件：

- [`firmware/GameBox_lcdwiki-es3c28p_v0.6.1-dev_merged.bin`](firmware/GameBox_lcdwiki-es3c28p_v0.6.1-dev_merged.bin)
- 烧录地址：`0x0`
- 文件大小：1,951,424 字节
- SHA-256：`86dbaa44dadf3cb4ea61e2e0c8276f542019af28b18b66881bfeefce108ac56a`

> 当前文件名中的 `dev` 表示开发测试版本。它已经包含 Bootloader、分区表、OTA 启动数据和主程序，可以用于空白设备的首次烧录，但正式交付用户前仍应完成真机回归测试。

该固件只适用于 LCDWIKI ES3C28P/ES3N28P。不要烧录到普通 ESP32、ESP32-C3 或项目早期使用的 ST7789 面包板主机。

## 2. 开始前需要准备什么

请准备：

1. 一台 LCDWIKI ES3C28P/ES3N28P GameBox 主机；
2. 一根支持数据传输的 USB 数据线；
3. Windows 电脑及乐鑫 Flash Download Tool；
4. 一张 FAT32 格式的 MicroSD 卡；
5. 可选的 ESP32-C3 无线手柄；
6. 一个 2.4 GHz Wi-Fi 网络。

> ESP32-S3 不支持连接仅开放 5 GHz 的 Wi-Fi。双频路由器通常可以直接使用，但必须开启 2.4 GHz 频段。

## 3. 首次烧录主机固件

### 3.1 打开烧录工具

推荐使用 Windows 版乐鑫官方 Flash Download Tool。启动时将 ChipType 选择为
`ESP32-S3`，其余启动选项保持工具默认值即可。

进入烧录页面后，只需要添加一个文件：

| 文件 | 地址 |
| --- | --- |
| `GameBox_lcdwiki-es3c28p_v0.6.1-dev_merged.bin` | `0x0` |

勾选文件左侧复选框，然后选择主机对应的串口。建议勾选
`DoNotChgBin`，让工具保留合并固件中已经写好的 Flash 参数。

![Flash Download Tool 示例](images/DIJI-NES_flash-download.png)

### 3.2 让主机进入下载模式

多数情况下，工具会自动让主机进入下载模式。如果一直显示等待连接或同步失败：

1. 按住主机的 `BOOT` 键；
2. 短按一次 `RESET` / `EN` 键；
3. 松开 `BOOT` 键；
4. 重新点击 `START`。

如果电脑没有出现串口，请先更换一根确认支持数据传输的 USB 线，再检查系统设备管理器中的串口状态。

### 3.3 开始烧录

1. 首次使用或需要彻底恢复时，先执行一次 `ERASE`；
2. 确认文件地址为 `0x0`；
3. 点击 `START`；
4. 等待工具显示 `FINISH`；
5. 按一次 `RESET`，或断电后重新上电。

擦除 Flash 会清除已保存的 Wi-Fi、手柄配对、设置和内部存档。普通 OTA 升级不需要擦除。

## 4. 准备 MicroSD 卡和游戏

建议先在电脑上将 MicroSD 卡格式化为 FAT32，然后建立以下目录：

```text
/rom
/saves
/wallpapers
```

将有权使用的 `.nes` 游戏文件复制到 `/rom`。支持在 `/rom` 下继续建立分类文件夹，设备会自动扫描子目录。

请在主机断电时插入或拔出 MicroSD 卡。开机后如果没有识别到 SD 卡，可进入“设置 → SD 卡”选择“重新检测”。

设备也提供 SD 卡格式化功能，但格式化会永久删除卡内的 ROM、存档、壁纸和截图。确认页面默认选择“取消”，只有明确选择“确认格式化”后才会执行。

> 本项目不附带商业游戏。请只使用自己制作、合法获得或明确允许再分发的 ROM。

## 5. 第一次开机

正常启动后，屏幕会先显示启动画面，然后进入壁纸时钟主页或游戏列表。

LCDWIKI 板型可以直接使用触摸屏。页面中高亮的按钮表示当前选中项目：

- 点击“游戏”进入本地游戏列表；
- 点击“设置”进入系统设置；
- 有最近游戏存档时，可以点击“继续”；
- 在游戏列表中点击游戏即可启动。

主机扩展接口还支持 `A`、`START`、`UP`、`DOWN` 四个实体按键，但要完整控制 NES 游戏，建议使用触摸控制或 ESP-NOW 无线手柄。

## 6. 连接家庭 Wi-Fi

1. 在主机上打开“设置 → WiFi 配网”；
2. 屏幕会显示一个类似 `GameBox-12AB34` 的临时热点；
3. 用手机连接该热点，热点默认没有密码；
4. 手机浏览器打开屏幕显示的地址，通常是 `http://192.168.4.1`；
5. 选择家庭 2.4 GHz Wi-Fi，输入密码并提交；
6. 等待主机显示“WiFi 连接成功并已保存”；
7. 点击屏幕底部“返回”结束配网。

如果手机提示该热点“无法访问互联网”，请选择继续使用当前网络，不要切回移动数据。连接失败时请确认密码、信号强度和 2.4 GHz 网络是否可用。

主机可以保存多组 Wi-Fi。以后开机时会优先连接最近成功使用的网络，并在必要时自动选择现场信号更好的已保存网络。

## 7. 配对 ESP32-C3 无线手柄

配套手柄首次需要单独烧录：

- [`firmware/GameBox_esp32c3-gamepad_v0.1.2_merged.bin`](firmware/GameBox_esp32c3-gamepad_v0.1.2_merged.bin)
- 烧录地址：`0x0`

完成手柄烧录后：

1. 主机进入“设置 → 游戏手柄”；
2. 选择 `P1` 或 `P2`；
3. 点击配对，或选中后按 `A`；
4. 在手柄上同时长按 `SELECT + START` 约 1.5 秒；
5. 手柄蓝灯快闪时正在配对；
6. 主机显示已配对并在线后即可使用。

手柄蓝色指示灯含义：

| 指示灯 | 状态 |
| --- | --- |
| 慢闪 | 尚未连接主机，正在寻找已配对主机 |
| 快闪 | 正处于配对模式 |
| 很慢轻闪 | 已连接，当前没有操作 |
| 常亮 | 已连接，正在按键操作 |

游戏列表顶部会显示手柄状态：

- `P1:ON 8ms`：P1 在线，当前单向通信延迟约 8 ms；
- `P1:ON --ms`：在线，正在完成时间同步；
- `P1:--`：已经配对，但当前离线；
- `P1:NO`：尚未配对。

手柄 5 分钟无操作后会自动休眠。当前接线可通过上、下、左、B 或 SELECT 键唤醒，唤醒后通常会自动恢复连接。

## 8. 用手柄操作系统界面

在时钟主页、游戏列表、商店和设置中统一使用以下规则：

| 按键 | 系统界面功能 |
| --- | --- |
| 方向键 | 移动焦点、选择项目或调整数值 |
| A | 确认、进入或执行当前高亮项目 |
| B | 返回上一级或取消 |
| SELECT | 在页面内容区与底部操作栏之间切换 |
| START | 执行当前页面的主要操作或确认当前项目 |

在游戏列表中：

- 上/下：选择游戏；
- A 或 START：启动游戏；
- SELECT：将焦点移到底部操作栏；
- B：返回时钟主页；
- 长按 B：删除当前可删除的本地游戏，删除前会再次确认。

进入游戏后，方向键、A、B、START、SELECT 恢复为标准 NES 手柄按键。同时按 `START + SELECT` 打开暂停菜单。

## 9. 存档、读取和倒带

暂停菜单提供三个手动存档槽：

1. 游戏中同时按 `START + SELECT`；
2. 上/下选择“保存”或“读取”；
3. 左/右选择槽 1、槽 2 或槽 3；
4. 按 A 或 START 确认；
5. 按 B 取消并返回游戏。

退出游戏时，系统还会保存最近进度和缩略图。带电池存档的游戏会定期保存 SRAM 数据。

在“设置 → 倒带时长”中可以选择关闭、5 秒、10 秒或 20 秒。启用后，在游戏中持续按住 `SELECT + 左` 即可倒带。

连接 Wi-Fi 后，本地存档会在后台同步到云端；网络暂时不可用不会影响本地保存。需要手动处理时，可以进入“设置 → 云端存档”执行立即同步或恢复。

## 10. 添加游戏和更换壁纸

除了直接读取 SD 卡，还可以使用主机提供的临时设置网页：

1. 进入主机“设置”；
2. 打开“网页管理”；
3. 如果主机已连接 Wi-Fi，用同一局域网内的手机或电脑打开屏幕显示的地址；
4. 如果主机未联网，先连接 `GameBox-Settings-XXXXXX` 热点，再打开屏幕显示的地址；
5. 在网页中进入“ROM 管理”或“壁纸管理”。

ROM 管理支持拖拽和多选上传 `.nes` 文件，单个文件不能超过 8 MB，同名文件不会自动覆盖。壁纸会在浏览器中裁切为适合屏幕的 320×240 JPEG。

临时网页只在系统设置打开期间运行。退出整个设置页面后，网页服务和临时热点会自动关闭；如果上传或删除过 ROM，主机会重新扫描游戏列表。

## 11. 游戏商店

主机连接 Wi-Fi 后，可以从底部操作栏进入“商店”：

- 上/下：浏览在线游戏；
- A 或 START：下载当前游戏；
- SELECT：切换到底部操作栏；
- B：返回本地游戏列表。

下载的游戏会保存到设备存储或 SD 卡，并经过完整性校验。商店中能够识别稳定游戏 ID 的游戏可以使用云存档功能。

## 12. 系统和手柄升级

以后正常升级不需要再次连接电脑烧录合并固件：

1. 保持主机连接家庭 Wi-Fi；
2. 保持需要升级的手柄已配对且在线；
3. 打开“设置 → 系统更新”；
4. 选择“检查更新”；
5. 发现版本后选择安装。

主机会同时上报自己的版本和在线手柄版本。只有服务器确认存在手柄新版本后，主机才会通知手柄升级。升级过程中不要断电、拔卡或让设备离开 Wi-Fi 覆盖范围。

首次烧录使用本教程中的 `merged.bin`；系统更新使用应用 OTA 固件。两者不能混用。

## 13. 关机和重新开机

进入“设置 → 关机设置”可以：

- 立即关机；
- 设置无操作 5、10 或 20 分钟后自动关机；
- 关闭自动关机。

LCDWIKI 板型进入软件关机后，长按触摸屏约 1.5 秒即可重新开机。无线手柄不能唤醒已经关机的主机，因为主机睡眠后无线模块也会关闭。

## 14. 常见问题

### 烧录工具一直等待连接

- 换用确认支持数据的 USB 线；
- 检查是否选择了正确串口；
- 按住 BOOT，短按 RESET/EN，再松开 BOOT；
- 将烧录波特率从 921600 降到 460800 或 115200；
- 关闭正在占用串口的串口监视器。

### 烧录成功但屏幕没有显示

- 确认固件文件名包含 `lcdwiki-es3c28p`；
- 确认文件烧录地址为 `0x0`；
- 烧录完成后按 RESET 或重新上电；
- 如果以前烧录过其他项目，先 ERASE 再重新烧录；
- 确认设备确实是 ES3C28P/ES3N28P，而不是其他屏幕型号。

### SD 卡没有识别

- 使用 FAT32 格式；
- 断电后重新插卡；
- 进入“设置 → SD 卡 → 重新检测”；
- 换用容量较小、质量可靠的 MicroSD 卡测试；
- 确保 ROM 扩展名为 `.nes`。

### 手柄配对不上

- 确认主机已经打开 P1/P2 配对页面；
- 同时按住 SELECT + START 满 1.5 秒；
- 看到蓝灯快闪后等待主机确认；
- 主机刚切换 Wi-Fi 信道时，等待几秒让手柄自动重新搜索；
- 主机和手柄协议版本跨度较大时，重新烧录两边当前固件。

### 手柄开始正常，后来延迟升高

- 进入“设置 → 游戏手柄”，选中手柄后按 START 运行 20 次通信测试；
- 平均 RTT 建议不高于 30 ms，最高 RTT 建议不高于 80 ms；
- 避免主机紧贴路由器、USB 3.0 硬盘或其他 2.4 GHz 干扰源；
- 下载游戏或执行 OTA 时会共享无线带宽，短时间波动属于正常现象。

### 关机后无法用手柄开机

这是正常设计。请长按主机触摸屏约 1.5 秒，手柄不能唤醒已经进入深度睡眠的主机。

## 15. 文件完整性校验

仓库提供校验清单：

- [`firmware/GameBox_initial_firmware_SHA256.txt`](firmware/GameBox_initial_firmware_SHA256.txt)

macOS 或 Linux 可以在项目 `firmware` 目录执行：

```bash
shasum -a 256 -c GameBox_initial_firmware_SHA256.txt
```

两个文件都显示 `OK` 后再进行烧录或分发。
