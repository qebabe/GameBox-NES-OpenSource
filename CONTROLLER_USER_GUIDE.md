# DIJI-NES 手柄操作说明

本文说明如何在不使用触摸屏的情况下，仅通过主机实体按键、串口控制器或
ESP-NOW 无线手柄操作 DIJI-NES 的全部系统界面和游戏功能。

## 1. 系统界面统一按键

在时钟主页、游戏列表、商店、设置和确认弹窗中，默认遵循以下规则：

| 按键 | 系统界面功能 |
| --- | --- |
| 方向键 | 移动焦点、选择条目或调整数值 |
| `A` | 确认、进入或执行当前高亮操作 |
| `B` | 返回上一级或取消 |
| `SELECT` | 在页面内容区和底部操作栏之间切换 |
| `START` | 执行当前页面的主要操作或确认当前焦点 |

页面中黄色、白色或高亮背景的边框表示当前焦点。按 `A` 或 `START` 时，
只会执行当前高亮的项目。

在系统界面中，P1 和 P2 两个 ESP-NOW 手柄都可以操作菜单。进入游戏后，
P1 恢复为一号玩家，P2 恢复为二号玩家。

## 2. 时钟主页

时钟主页底部可能显示：

- `继续`：读取最近一次游戏的自动存档。
- `游戏`：进入游戏列表。
- `设置`：进入系统设置。

操作方法：

| 按键 | 功能 |
| --- | --- |
| 左/右 | 在“继续、游戏、设置”之间移动 |
| `A` | 打开当前高亮项目 |
| `START` | 快速进入游戏列表 |
| `B` | 有最近游戏时快速继续 |

没有可继续的游戏时，不会选中“继续”。

## 3. 游戏列表

### 3.1 选择和启动游戏

| 按键 | 功能 |
| --- | --- |
| 上/下 | 在游戏列表中选择游戏 |
| `A` | 启动当前游戏 |
| `START` | 快速启动当前游戏 |
| `B` | 返回时钟主页 |
| 长按 `B` | 删除当前选中的可删除本地游戏 |
| `SELECT` | 将焦点切换到底部操作栏 |

删除游戏时会先显示确认窗口。默认焦点位于“取消”：

- 左/右：选择“取消”或“确认”。
- `A`：执行当前选择。
- `B`：取消。

### 3.2 底部操作栏

底部操作栏包含：

1. 主页
2. 设置
3. 商店
4. 开始
5. 关机

操作方法：

| 按键 | 功能 |
| --- | --- |
| `SELECT` | 从游戏列表切换到底栏，或从底栏返回游戏列表 |
| 左/右 | 在底栏项目之间移动 |
| 上 | 返回游戏列表 |
| `A` | 执行当前高亮项目 |
| `START` | 执行当前高亮项目 |
| `B` | 从底栏返回游戏列表；再次按 B 返回主页 |

如果列表中没有游戏，“开始”会重新检测存储设备。

## 4. 游戏商店

### 4.1 游戏列表区

| 按键 | 功能 |
| --- | --- |
| 上/下 | 选择在线游戏 |
| `A` | 下载当前游戏 |
| `START` | 快速下载当前游戏 |
| `B` | 返回游戏列表 |
| `SELECT` | 切换到底部操作栏 |

### 4.2 商店底部操作栏

底栏包含：

1. 更新：重新联网获取商店列表。
2. 下载：下载当前游戏。
3. 返回：退出商店。
4. 顶部：跳到列表顶部。
5. 更多：加载更多项目并跳到当前列表底部。

底栏聚焦后使用左/右选择，使用 `A` 或 `START` 执行。按上、`SELECT`
或 `B` 可以回到商店列表区。

## 5. 系统设置

系统设置主列表使用：

| 按键 | 功能 |
| --- | --- |
| 上/下 | 选择设置项目 |
| `A` / `START` | 进入或切换当前设置 |
| `B` | 返回上一页 |
| `SELECT` | 在设置列表与底部“返回”之间切换 |

设置项目包括 Wi-Fi、游戏手柄、显示模式、音量、触摸校准、开机主页、
壁纸、网页管理、倒带时长、关机设置、SD 卡、云端存档和系统更新。

没有触摸控制器的设备会把“触摸校准”显示为“不可用”，不会进入校准流程。

### 5.1 音量

- 左或下：降低音量。
- 右、上、A 或 START：提高音量。
- B：返回。

### 5.2 手柄配对

- 上/下：选择 P1 或 P2。
- A：开始配对。
- SELECT：测试当前手柄震动。
- START：运行 20 次通信 RTT 测试。
- B：返回。

进入配对后，同时长按无线手柄的 `SELECT + START` 约 1.5 秒。

### 5.3 壁纸

- 方向键：在壁纸缩略图之间移动。
- A 或 START：应用当前壁纸。
- B：返回。

### 5.4 云端存档

页面包含“立即同步”“从云端恢复”和底部“返回”：

- 左/右：在两个主要操作之间选择。
- 下或 SELECT：把焦点移到“返回”。
- 上或 SELECT：从“返回”回到主要操作。
- A 或 START：执行当前高亮操作。
- B：返回。

“从云端恢复”要求设备曾经启动过需要恢复存档的游戏。

### 5.5 SD 卡

页面包含“重新检测”“格式化 SD 卡”和底部“返回”：

- 左/右：选择重新检测或格式化。
- 下或 SELECT：把焦点移到“返回”。
- A 或 START：执行当前高亮操作。
- B：返回。

格式化会永久删除 SD 卡中的 ROM、存档、壁纸和截图。确认窗口默认选择
“取消”，必须先用左/右把焦点移到“确认格式化”，再按 A。

### 5.6 系统更新

- 左/右或 SELECT：在“检查更新”和“返回”之间选择。
- A 或 START：执行当前选择。
- B：返回。

发现可选更新时，确认窗口默认选择“取消”。必须先选择“安装”，再按 A。

### 5.7 关机设置

- 左/右：选择自动关机时间。
- 下：选择“立即关机”。
- 上：返回自动关机选项。
- A 或 START：应用当前选择。
- B：返回。

关机确认窗口默认选择“取消”。需要用左/右选择“关机”，再按 A。

没有触摸唤醒引脚或其他深度睡眠唤醒按键的设备，会自动禁用自动关机和
立即关机，避免设备关机后无法重新启动。

## 6. 游戏运行

进入游戏后，手柄按键恢复为 NES 标准按键：

| 手柄按键 | 游戏功能 |
| --- | --- |
| 方向键 | 游戏方向 |
| A | NES A |
| B | NES B |
| START | NES START |
| SELECT | NES SELECT |

P1 控制一号玩家，P2 控制二号玩家。

### 6.1 打开暂停菜单

游戏中同时按 `START + SELECT` 打开暂停菜单。

暂停菜单操作：

| 按键 | 功能 |
| --- | --- |
| 上/下 | 选择“继续、保存、读取、返回菜单” |
| 左/右 | 调整音量；在保存/读取项上切换存档槽 |
| `A` / `START` | 执行当前项目 |
| `B` | 返回游戏 |

保存或读取时：

- 左/右：选择槽 1、槽 2 或槽 3。
- A 或 START：确认。
- B：取消并返回暂停菜单。

### 6.2 倒带

设置中启用倒带后，在游戏中持续按住 `SELECT + 左` 执行倒带。

## 7. 无触屏设备注意事项

系统菜单和游戏功能可以完全通过实体按键、串口控制器或 ESP-NOW 手柄操作。
但以下功能仍依赖外部设备：

- Wi-Fi 首次配网需要手机连接主机热点并在浏览器提交 Wi-Fi 信息。
- 网页 ROM 和壁纸管理需要手机或电脑浏览器。
- 软件关机后的唤醒需要主机上的触摸中断或专用物理唤醒按键。

ESP-NOW 手柄不能唤醒已经进入深度睡眠的主机，因为主机睡眠后无线模块已经
关闭。没有可用唤醒脚的硬件不会进入软件深度关机。

## 8. 手柄状态和延迟

游戏列表顶部显示 P1/P2 状态：

- `P1:ON 8ms`：手柄在线，单向通信延迟约 8ms。
- `P1:ON --ms`：手柄在线，正在完成时间同步。
- `P1:--`：已经配对但当前离线。
- `P1:NO`：尚未配对。

顶部延迟用于判断“手柄采样到主机接收”的无线通信时间，不包含游戏下一帧
处理、画面传输和液晶响应时间。

## 9. 手柄自动休眠与唤醒

无线手柄连续 5 分钟没有任何按键操作后会自动进入低功耗休眠。休眠后主机
上的 P1/P2 状态会显示为离线，这是正常现象。

按当前 ESP32-C3 接线，按以下任意一个键可以重新启动手柄：

- 上
- 下
- 左
- B
- SELECT

手柄唤醒后会自动恢复原来的配对信息和无线信道，一般不需要重新配对。
由于 GPIO 深睡唤醒限制，当前的 A、START 和右键不能用于唤醒，但唤醒后均
可正常使用。
